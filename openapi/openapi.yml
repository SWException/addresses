openapi: 3.0.3
info:
  title: EmporioLambda BE
  description: OpenAPI for addresses management
  version: 0.1.0

servers:
- description: SwaggerHub API Auto Mocking
  url: https://virtserver.swaggerhub.com/swexception4/OpenAPI/0.1.0/addresses
- description: serverless offline
  url: http://localhost:4000/dev/addresses

paths:
  /addresses:
    $ref: "addresses.yml"
  /addresses/{id}:
    $ref: "addressesByID.yml"